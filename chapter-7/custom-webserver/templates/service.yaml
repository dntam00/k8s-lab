apiVersion: v1
kind: Service
metadata:
  name: {{ include "custom-webserver.fullname" . }}
  labels:
    {{- include "custom-webserver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "custom-webserver.selectorLabels" . | nindent 4 }}
