apiVersion: v1
kind: Service
metadata:
  name: secondapp
  labels:
    app: secondapp
spec:
  ports:
  - port: 80
    protocol: TCP
  selector:
    app: secondapp
  sessionAffinity: None
status:
  loadBalancer: {}
